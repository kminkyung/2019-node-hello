ul.pager.d-flex.my-4.justify-content-center(data-pager-name=`${pager.link}`)

	if pager.prevShow
		li.page-first(data-page=`${pager.prev}`, style="cursor: pointer;")
			span.page-link
				i.fas.fa-angle-double-left
	else
		li.page-first.disabled
			span.page-link
				i.fas.fa-angle-double-left

	if pager.page > 1
		li.page-prev(data-page=`${Number(pager.page) - 1}`, style="cursor: pointer;")
			span.page-link
				i.fas.fa-angle-left
	else
		li.page-prev.disabled
			span.page-link
				i.fas.fa-angle-left
			
	- for(var i=pager.stn; i<=pager.edn; i++) {
		if pager.page == i
			li.page-ct.active
				span.page-link= i
		else
			li.page-ct(data-page=`${i}`, style="cursor: pointer;")
				span.page-link= i
	- }

	if pager.page < pager.cnt
		li.page-next(data-page=`${Number(pager.page) + 1}`, style="cursor: pointer;")
			span.page-link
				i.fas.fa-angle-right
	else
		li.page-next.disabled
			span.page-link
				i.fas.fa-angle-right
			

	if pager.lastShow
		li.page-last(data-page=`${pager.next}`, style="cursor: pointer;")
			span.page-link
				i.fas.fa-angle-double-right 
	else
		li.page-last.disabled
			span.page-link
				i.fas.fa-angle-double-right 

			